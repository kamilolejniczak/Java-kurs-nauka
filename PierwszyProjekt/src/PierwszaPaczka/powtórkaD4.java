package PierwszaPaczka;

import java.util.HashMap;
import java.util.Scanner;

public class powtórkaD4 {

	public static void main(String[] args) {
		// TODO Auto-generated method stub

		
		/*Random los = new Random();
		
		int[][] macierz = new int[5][5];
		
		for(int i=0; i<5; i++) {
			for(int j=0; j<5; j++) {
				macierz[i][j] = los.nextInt(11)-5;
			
			System.out.print(macierz[i][j]+"\t");
			}
			System.out.println("");
		}
		
		int macMax = macierz[0][0];
		int macMin = macierz[0][0];
		int[][] maxMin = new int[2][5];
		for(int j=0; j<macierz.length; j++) {
			for(int i=0; i<macierz[j].length; i++) {
				if(macMin<macierz[i][j]) {
					macMin = macierz[i][j];
				}
				if(macMax>macierz[i][j]) {
					macMax = macierz[i][j];
				}
			}
			maxMin[0][j] = macMax;	
			maxMin[1][j] = macMin;
		}
		System.out.println("");
		for(int i=0; i<maxMin.length; i++) {
			for(int j=0; j<maxMin[i].length; j++) {
				System.out.print(maxMin[i][j] + "\t");
			}
			System.out.println("");
		}*/
		
		/*Scanner input = new Scanner(System.in);
		System.out.println("wpisz znak, (end)-wyjœcie: ");
		ArrayList<String> lista = new ArrayList<String>();
		
		String s = input.nextLine();
		
		while(!s.equals("end")) {
			lista.add(s);
			s = input.nextLine();
		}
		
		System.out.println(lista);
		input.close();*/
		
	/*	Scanner input = new Scanner(System.in);
		HashMap<String,Float> sklep = new HashMap<String,Float>();
		
		sklep.put("mleko", (float)2.7f);
		sklep.put("jajka", (float)0.70f);
		sklep.put("chleb", (float)3f);
		sklep.put("m¹ka", (float)1.80f);
		sklep.put("cukier", (float)2.99f);
		sklep.put("bu³ki", (float)0.45f);
		sklep.put("pomidory", (float)5.99f);
		sklep.put("woda", (float)1.60f);
		sklep.put("pomarañcze", (float)4.99f);
		sklep.put("ser", (float)3.30f);
		
		System.out.println("jajka kosztuj¹ " + sklep.get("jajka") + "z³");
		
		float sum = 0;
		String s = input.nextLine();
		while(!s.equals("koniec")) {
			if(sklep.containsKey(s)) {
				sum += sklep.get(s);
			}
			else if(sklep.containsKey(s)) {
				String ss = input.nextLine();
			}
			else {
				System.out.println("z³a nazwa produktu");
			}
			s = input.nextLine();
		}
		System.out.println("suma: " + sum);
		input.close();*/
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
		
	}

}
